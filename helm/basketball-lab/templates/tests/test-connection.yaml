apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basketball-lab.fullname" . }}-test-connection"
  labels:
    {{- include "basketball-lab.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basketball-lab.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
