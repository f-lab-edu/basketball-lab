apiVersion: v1
kind: Service
metadata:
  name: {{ include "basketball-lab.fullname" . }}
  labels:
    {{- include "basketball-lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basketball-lab.selectorLabels" . | nindent 4 }}
